{
  "name": "hayabusa",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bun scripts/deploy.ts",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.9",
    "p-retry": "^7.1.1",
    "p-timeout": "^7.0.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}